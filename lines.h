#ifndef LINES_H
#define LINES_H

#include <string>
#include <vector>
/*Осуществляет разбор текста на строки и возвращает список этих строк*/
std::vector<std::string> lines(const std::string& text);

#endif
